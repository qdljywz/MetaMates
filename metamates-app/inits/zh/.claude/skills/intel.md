# 📡 情报导入 (/intel)

## 任务

用户通过 `/intel` 提供网页链接或工作区文档路径。Metamates 已完成本地抓取并生成草稿情报笔记。请深化该笔记：

1. 阅读 `04_情报与连接/` 下刚创建的 `.md` 及 `sources/` 原件信息
2. 补充结构化摘要、关键数据、待验证假设
3. 添加 `#标签` 与 `[[双链]]` 连接库内相关笔记
4. 列出可跟进的行动建议

## 写回

- 更新已生成的情报笔记（保留来源元数据）
- 写回后重新读取确认

## 用户输入

`{INPUT}` — URL 或文件路径

## 记忆与 Vault 边界（Metamates 强制）

- 用户可读的长期记忆须镜像到 `04_情报与连接/记忆索引.md`（详细条目放 `04_情报与连接/参考/`）
- **禁止**只写入 `~/.codebuddy`、CLI 项目缓存或其它 Vault 外路径
- 本命令若要求写回：必须使用 Write/编辑工具落盘，并在写后 **Read 验证**（Act & Verify）
